---
interface Props {
  date: Date
  language?: string
}

const { date, language = 'es-ES' } = Astro.props

const text = new Intl.DateTimeFormat(language, {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
}).format(date)
---

<time datetime={date.toISOString()}>
  {text}
</time>
